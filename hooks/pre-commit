#!/bin/sh

echo "Running ktlint check..."

./gradlew :app:ktlintCheck

RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo "Ktlint found errors! Run './gradlew ktlintFormat' to fix them."
  exit 1
fi

echo "✅ ktlint passed"
